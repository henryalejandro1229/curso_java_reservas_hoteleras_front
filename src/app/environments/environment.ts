export const localhost: string = 'http://localhost';

export const environment = {
  apiUrl: `${localhost}:8090/api`,
  authUrl: `${localhost}:9000/api/login`,
  apiUsuarios: `${localhost}:9000/admin/usuarios`,
};